<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prairie Dogs</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <div class="row justify-content-center my-4">
            <div class="col text-center">
                <img src="/images/pdog1.jpg" alt="a prairie dog" style="width: 300px; height: 300px;">
            </div>
        </div>
        <div class="row">
            <h1 class="col text-center display-4">Prairie Dogs on the Prairie</h1>
        </div>
        <hr>
        <% for(var prairie of prairies) { %>
            <div class="row align-items-center my-4">
                <div class="lead col-md-3 text-right">
                    <span>&#8226;</span>
                    <a href="/prairies/<%= prairie._id %>">
                        <img class="ml-2" src="<%= prairie.image %>" alt="<%= prairie.name %> the prairie dog" style="width: 150px; height: 150px;">
                    </a>
                </div>
                <p class="lead col-md-4 text-left"><%= prairie.name %></p>
                <form class="offset-md-3 col-md-2" action="/prairies/<%= prairie._id %>/destroy" method="POST">
                    <input class="form-control btn btn-danger" type="submit" value="Remove">
                </form>
            </div>
        <% } %>
        <hr>
        <a href="/prairies/new">Add a new Prairie Dog</a>
    </div>
</body>
</html>